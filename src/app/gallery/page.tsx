import { PhotoGallerySection } from "@/components";

export const metadata = {
  title: "Gallery | May Nail Spa",
  description: "Photo gallery of our nail art and salon.",
};

export default function GalleryPage() {
  return <PhotoGallerySection />;
}
